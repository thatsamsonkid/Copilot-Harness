# Jira Cloud basic auth (email + API token).
# Create a token using docs/jira-api-token.md
# https://id.atlassian.com/manage-profile/security/api-tokens
#
# Preferred: store the API token in the OS keychain
# (macOS Keychain or Windows Credential Manager):
#   uv run harness jira login
# JIRA_API_TOKEN here is a fallback for CI or machines without a keychain.
#
# Do not source this file in your shell and do not paste these values into chat.
# The harness CLI loads .env in-process. Copilot should only run `uv run harness`.
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=you@company.com
JIRA_API_TOKEN=

# Optional. Override auto-detection of this repository root.
# HARNESS_ROOT=/absolute/path/to/Copilot-Harness
